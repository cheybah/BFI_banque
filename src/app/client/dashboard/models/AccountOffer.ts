export interface AccountOffer {
    agencyName: string;
    accountType: string; // Change to match your enum in the backend
    packType: string; // Change to match your enum in the backend
    user?: any; 
  }
  